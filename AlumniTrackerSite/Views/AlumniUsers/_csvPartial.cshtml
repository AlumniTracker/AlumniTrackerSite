@*Lets the admin download a CSV from the search page*@
@using(@Html.BeginForm("CSV", "AlumniUsers", FormMethod.Post)) {
    <input type="submit" name="btnCSV" id="CSV" class="navi" value="Download CSV"/>
    
}